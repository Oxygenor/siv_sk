# Стан проєкту — редизайн сайту КЗ «Сиваковецька гімназія»

> Нотатка для себе (Claude) на завтра. Читай це першим, коли повернешся до O:\siteSchool.

## 🔜 В процесі 2026-09-24 — заміна "буде додано найближчим часом" на реальні документи

Користувач попросив прибрати написи-заглушки на сайті. Єдине таке місце — бейдж
**«Буде додано найближчим часом»** у `DocumentList.jsx:23`, який показується для
кожного пункту `src/data/documents.js`, поки в ньому не вказано `filename`.
Домовленість з 2026-09-22: користувач кидає файли в `public/documents/`, я
дописую `filename`.

**Зроблено 2026-09-24:** користувач додав 3 PDF, підв'язав до слотів (лінкуємо
тільки PDF, не DOCX — компактніше на телефоні):
- `pravyla-povedinky` → `правила поведінки.pdf`
- `kryterii-ocinyuvannya` → `критерії оцінювання.pdf`
- `pravyla-pryyomu` → `Правила прийому до закладу освіти.pdf`

Build/lint чисті, посилання перевірено (`vite preview` + curl на закодований
URL → 200). Також видалено `public/staff_full/` — випадково завантажена копія
фото персоналу в повній роздільності, дублікат уже стиснутих `public/staff/`,
ніде в коді не використовувалась.

**Залишається:** розклад уроків. У `public/documents/` вже лежать
`rozklad_1-4_klasy.pdf/docx` і `rozklad_5-9_klasy.pdf/docx` (два окремі файли
на два вікові рівні), а в `documents.js` — лише один слот `rozklad-urokiv`.
Користувач попросив **відкласти це на потім** — не вирішено ще, чи розбивати
на 2 слоти («Розклад уроків 1-4 класи» / «5-9 класи»), чи лишати один слот з
двома посиланнями всередині. Файли вже на місці, чекають рішення. **Також є
готовий `driveUrl` для розкладу зі старого сайту** (файл «розклад 23-24.docx»,
`drive.google.com/file/d/1UQvRCZArKQDsE5MCMfKVxgNqV1HCoaSX/view`, перевірено —
публічний) — можна використати замість/на додачу до локальних файлів, коли
дійдемо до цього слота.

## ✅ Зроблено 2026-09-24 (продовження) — `driveUrl` для решти документів

Користувач запропонував: для документів, чиї файли залишились лише на
Google Drive старого сайту (частина була заблокована для автоматичного
витягування — див. аудит нижче), не переносити файл локально, а просто
лінкувати на сам Drive. Реалізовано:

- **Код:** `DocumentList.jsx` тепер приймає або `filename` (локальний файл у
  `public/documents/`), або `driveUrl` (пряме посилання на Drive) — якщо є
  обидва, пріоритет локальному файлу. Кнопка підписується «Завантажити» чи
  «Переглянути в Google Drive» відповідно. Задокументовано у шапці
  `documents.js`.
- **Знайдені й перевірені (публічний доступ, назва файлу на Drive збігається
  з очікуваною) посилання, додані в код:**
  - `statut-licenziya` (Статут та ліцензія) → папка Drive
  - `osvitnya-programa` (Освітня програма) → папка Drive
  - `materialno-tehnichne` (Матеріально-технічне забезпечення) → файл
  - `shtatnyi-rozpys` (Штатний розпис) → файл
  - `plan-zahodiv-buling` (Стоп булінг — план заходів) → файл
  - `poryadok-zayav-buling` (Порядок подання заяв про булінг) → файл
  - `rozklad-urokiv` (Розклад уроків) → файл знайдено, **не застосовано**
    (слот відкладено, див. вище)

**Метод пошуку:** прямий WebFetch на сирий HTML старого сайту бачив лише 2 з
15 (ті, де Google Sites вставляє звичайне посилання-папку без JS-рендеру).
Для решти довелось піднімати Playwright (headless chromium, рендер JS) і
шукати `iframe[src*=drive.google.com]` / `a[href*=drive.google.com]` у
відрендереному DOM — так знайшлись файлові (не папкові) посилання.

**Не вдалось знайти (сторінка існує, але в DOM немає drive.google.com-посилання
— ймовірно контент там як зображення на googleusercontent.com, а не
вбудований документ):** Протидія Булінгу, Психологічна підтримка, Фінансова
звітність, Територія обслуговування закладу, Звіт керівника закладу.

**Не вдалось знайти (сторінка за здогаданим шляхом відповідає 404 — потрібен
ручний пошук реального шляху через навігацію сайту, автоматично не пробував
через ліміт запитів розвідки):** Річний план роботи, Положення про
академічну доброчесність, Кадрове забезпечення, Стратегія розвитку закладу,
Фінансово-господарська діяльність.

**Ще не шукали взагалі (не було в списку розвідки):** Режим роботи закладу,
Структура навчального року, Ліцензований обсяг, Мова освітнього процесу,
Структура управління закладу, Моніторинг якості освіти, Атестація учителів,
Обов'язки здобувачів освіти (текст уже на сайті, документ не потрібен),
Організація харчування, Covid-19, Вибір підручників, Умови доступності для
дітей з особливими потребами.

Build/lint чисті після кожної зміни.

**Побічна знахідка:** розвідувальний Playwright-агент (усупереч інструкції
нічого не завантажувати) сам зберіг `public/documents/Територія
обслуговування закладу.jpg` — це виявилась не карта (як очікувалось з
аудиту), а скан офіційної довідки Турбівської селищної ради про те, які
села обслуговує заклад. Перевірив вручну — легітимний документ, підв'язав
як `filename` для слота `terytoriya-obslugovuvannya`.

## ✅ Зроблено 2026-09-24 (продовження 2) — сторінка-розділ для "документів-фото"

Слот «Умови доступності закладу для дітей з особливими потребами» — не один
файл, а 4 фото (за аудитом старого сайту). Користувач обрав показ: одна
кнопка в списку документів → окрема сторінка з фото (як `Sections.jsx`/
`CustomSection.jsx`, вже існуючий механізм довільних розділів `/rozdily/:slug`).

Реалізовано:
- `src/data/documents.js` — новий 3-й спосіб посилання на слот: `pageSlug`
  (пріоритет `filename` > `driveUrl` > `pageSlug`). Застосовано до
  `dostupnist-osoblyvi-potreby`.
- `src/data/sections.js` — новий масив `photos` (назви файлів з
  `public/sections/`) поряд з існуючими `body`/`filename`. Додано запис
  `dostupnist-osoblyvi-potreby` (поки `photos: []` — чекаю фото від
  користувача).
- `CustomSection.jsx` — рендерить сітку фото (`.section-photo-grid`, клік
  відкриває оригінал у новій вкладці), якщо `photos` не порожній; `body`
  тепер рендериться лише коли не пустий рядок.
- `DocumentList.jsx` — підтримує `pageSlug`, кнопка «Переглянути» веде
  внутрішнім `<Link>` на `/rozdily/:slug`.
- Створено `public/sections/.gitkeep`, чекає на 4 фото від користувача (він
  сказав, що завантажить сам).

Build/lint чисті, `/dokumenty` і `/rozdily/dostupnist-osoblyvi-potreby`
перевірено через `vite preview` (обидва 200).

**Наступний крок:** коли користувач кине 4 фото в `public/sections/`, дописати
їхні назви в `photos: []` запису `dostupnist-osoblyvi-potreby`.

## ✅ Зроблено 2026-09-24 (продовження 3) — фото доступності + Covid-19 текст

Користувач завантажив 4 фото (`1.jpg`–`4.jpg`) у `public/sections/`, дописано в
`photos: []` запису `dostupnist-osoblyvi-potreby`. Перевірено Playwright —
сторінка `/rozdily/dostupnist-osoblyvi-potreby` рендерить усі 4. Комміт `3880f36`.

**Covid-19 — рекомендації:** на оригінальному сайті цієї сторінки не було
взагалі (аудит підтвердив: порожньо в HTML). Користувач попросив додати
офіційну інформацію самостійно. Написав власний текст (не сканований
"документ" — щоб не видавати за офіційний бланк, якого не існує), спираючись
на реальні факти з пошуку: карантин в Україні скасовано постановою КМУ №651
від 27.06.2023, актуальні рекомендації МОЗ — вакцинація (безоплатна, для
більшості достатньо 1 дози на сезон), плюс стандартна гігієна/провітрювання/
залишатись вдома при симптомах. Реалізовано як текстовий запис `covid-19` у
`sections.js` (без фото — не було легітимного зображення, яке варто було б
видавати за офіційне), підключено через `pageSlug` у `documents.js`.
Перевірено Playwright-рендером — текст на сторінці є. Build/lint чисті.

## ✅ Зроблено 2026-09-24 (продовження 4) — розклад уроків підключено

Останній слот-заглушка. Користувач підтвердив: розбити на 2 окремі пункти
списку («Розклад уроків 1-4 класи» / «5-9 класи», кожен своя кнопка
«Завантажити»), лінкувати тільки PDF (як і для решти документів). Змінено
`slug: 'rozklad-urokiv'` → два нові слоти `rozklad-urokiv-1-4` /
`rozklad-urokiv-5-9` у `documents.js`; довелось оновити фільтр `slots(...)` у
`Students.jsx:25`, який досі посилався на старий одиночний slug. Перевірено:
build/lint чисті, обидва PDF резолвляться (200) через `vite preview`, сторінка
`/uchnyam` віддає 200.

**Розклад більше не заглушка** — це закривало останнє з того, що просив
користувач у цій сесії. Решта незаповнених слотів (~19, повний список — у
розділі "Не вдалось знайти" вище) досі показує заглушку "буде додано
найближчим часом", чекають або файлів від користувача, або нового
розвідувального проходу по навігації старого сайту.

## ✅ Зроблено 2026-09-24 (продовження 5) — «Обов'язки здобувачів освіти»

Користувач попросив дописати текст для слота `obovyazky-zdobuvachiv` (дав
скорочений варіант — 2 пункти, які вже й так є на `Students.jsx` окремим
`<ul>`) і дозволив або взяти "офіційний" варіант, або написати свій.
Знайшов через WebSearch/WebFetch повний офіційний текст — стаття 53 частина 3
Закону України «Про освіту» (5 пунктів, включно з тими двома, що навів
користувач) — і вставив його як текстову сторінку-розділ (той самий підхід,
що й для Covid-19: `sections.js` + `pageSlug` у `documents.js`, без фото —
це цитата закону, а не скан). Перевірено Playwright — сторінка
`/rozdily/obovyazky-zdobuvachiv` рендерить текст, каталог `/dokumenty`
показує пункт з активним посиланням. Build/lint чисті. **Не чіпав** наявний
короткий `<ul>` на `Students.jsx:36-46` — там уже точна цитата 2 з 5 пунктів,
лишив як є, повний текст лежить окремою сторінкою для тих, кому треба
деталі.

## ✅ Зроблено 2026-09-24 (продовження 6) — «Режим роботи закладу» + виправлено CSS-баг

Користувач дав графік роботи (пн-пт 09:00–17:00, вихідні зачинено). Замість
plain-text вирішив зробити красиво (сам попросив "оформи на свій погляд"):
додав новий тип поля `hours` у `sections.js` (масив `{ day, time }` або
`{ day, closed: true }`) і рендер стилізованою таблицею в `CustomSection.jsx`
з підсвіткою поточного дня (`new Date().getDay()`) і приглушеним "Зачинено".

**Під час перевірки скриншотом знайшов і виправив баг:** стилі
`.section-photo-grid`/`.hours-table` (файл `news-detail.css`) імпортувались
лише в `NewsDetail.jsx`, а не в `CustomSection.jsx` — тобто сітка фото для
«Умов доступності» (додана раніше) весь цей час рендерилась БЕЗ стилів
(картинки йшли одна під одною на всю ширину замість акуратної сітки).
Додав імпорт `news-detail.css` у `CustomSection.jsx`. Заодно виправив
конфлікт: глобальний `table { min-width: 480px }` перебивав `max-width: 420px`
у `.hours-table` — додав `min-width: 0` для перевизначення.

Перевірено скриншотами (Playwright, 500px viewport) — обидві сторінки тепер
виглядають коректно: таблиця графіка з підсвіченим четвергом, фотосітка
доступності — 2 колонки із заокругленими кутами. Build/lint чисті.

## ✅ Зроблено 2026-09-24 (продовження 7) — ще 3 Drive-посилання, з'ясовано що решта справді порожні

Користувач підказав формат посилання (`drive.google.com/drive/u/0/folders/...`)
на прикладі «Освітньої програми» — виявилось, це той самий ID, що вже
підключений раніше. Запустив повторну розвідку, цього разу з повним обходом
реальної навігації старого сайту (не здогадані шляхи) — знайдено й перевірено
(публічний доступ, назва файлу збігається):
- `richnyi-plan` (Річний план роботи закладу) → файл
- `akademichna-dobrochesnist` (Положення про академічну доброчесність) → файл
- `strategiya-rozvytku` (Стратегія розвитку закладу) → файл

**Важливе з'ясування:** для 13 сторінок (Фінансова звітність, Звіт керівника,
Фінансово-господарська діяльність, Протидія Булінгу, Психологічна підтримка,
Структура навчального року, Ліцензований обсяг, Мова освітнього процесу,
Структура управління закладу, Моніторинг якості освіти, Атестація учителів,
Організація харчування, Вибір підручників) — це **не проблема інструменту**:
сторінки в навігації реальні, але показують порожній блок «Embedded Files.»
без жодного прикріпленого файлу навіть у відрендереному DOM. Тобто на
старому сайті туди ніколи не прикріпляли документ — витягувати нічого,
доведеться або запитати контент напряму в адміністрації, або написати
власний текст (як для Covid-19/обов'язків).

**Кадрове забезпечення** — такої сторінки в навігації старого сайту взагалі
немає (не помилка шляху — підтверджено відсутність у меню).

Build/lint чисті після кожної зміни.

## ✅ Зроблено 2026-09-24 (продовження 8) — «Мова освітнього процесу»

Один із 13 слотів, підтверджених вище як реально порожні на старому сайті.
Користувач попросив описати самостійно: мова — українська. Додано текстовим
розділом (`sections.js` + `pageSlug`), з посиланням на ст. 10 Конституції
України та ст. 7 Закону України «Про освіту» (державна мова — мова
освітнього процесу). Build/lint чисті.

## ✅ Зроблено 2026-09-22 — виправлено HTTP 404 на статичних сторінках

Користувач побачив у консолі браузера `GET /pro-gimnaziyu 404 (Not Found)`.
Playwright підтвердив: контент рендерився коректно, але сервер справді
віддавав HTTP-статус 404 (побічний ефект трюка `index.html → 404.html` для
SPA на GitHub Pages без серверних рерайтів) — ризик для SEO, бо Google може
трактувати 404 як "сторінки не існує". Виправлено в `.github/workflows/deploy.yml`:
після білда для кожного статичного маршруту (`pro-gimnaziyu`, `novyny`,
`uchnyam`, `batkam`, `dokumenty`, `biblioteka`, `formy-navchannya`,
`fotogalereya`, `korysni-posylannya`, `kontakty`, `poshuk`, `rozdily`)
створюється реальна папка з власним `index.html` — GitHub Pages віддає її
напряму зі статусом 200 (з 301-редиректом без-слеша→зі-слешем, це нормально).
Перевірено на живому сайті через Playwright, включно зі сценарієм із якорем
(`/pro-gimnaziyu#administratsiya`) — прокрутка й видимість секції не зламались.
Динамічні маршрути (`/novyny/:id`, `/rozdily/:slug`) досі йдуть через
404-фолбек (їхні адреси невідомі на етапі білда) — не критично, поки там
немає реального контенту. Коміт `7e14363`, задеплоєно й перевірено.

## ✅ Зроблено 2026-09-21 — Google Business Profile створено, лінк на сайті

Користувач самостійно створив Google Business Profile (business.google.com) для
закладу — головний важіль для локальних запитів типу «школа Сиваківці», раніше
рекомендований, але не зроблений. NAP (назва/адреса/телефон) звірено з
`src/data/school.js` — збігається, правок не було. Додано поле
`school.googleProfile` (`https://share.google/po9YbjVjNohW0IYL8`) і посилання
«Профіль на Google» на сторінці `/kontakty` поруч із Facebook-групою. Build/lint
чисті. Коміт `3524a01`, запушено.

**Залишається з попередньої SEO-поради (2026-08-31):**
- Зворотні посилання зі старого сайту/Facebook-групи/сайту ОТГ на
  syvakovetska.vn.ua — домен ще новий, довіри пошуковика ще нема. Обговорено
  2026-09-21: визначено пріоритетом (впливає на весь сайт, не одну сторінку).
  Користувач підтвердив, що має доступ до всіх трьох акаунтів (старий Google
  Sites, FB-група, сайт ОТГ), додасть посилання **пізніше сам** — дій поза
  кодом, з мого боку нічого не потрібно, просто чекаємо.
- Наповнити `news.js` реальним контентом — досі порожній, свідомо відкладено
  на другий план цього разу (немає готового контенту).

## ✅ Зроблено 2026-08-31 — фавікон, SEO-формулювання

- **Фавікон.** Був дефолтний `favicon.svg` від Vite — і у вкладці браузера, і
  в пошуку Google показувалась не іконка гімназії. Замінено на
  `public/iconSite.png` (той самий логотип, що вже використовується як
  OG-прев'ю) для `<link rel="icon">` і `apple-touch-icon`; `favicon.svg`
  видалено як більше не використовуваний. **Важливо:** іконка в самій видачі
  Google оновлюється з великою затримкою (дні-тижні) навіть після деплою —
  можна прискорити через Search Console → Інспектування URL → «Запросити
  індексування». Також PNG важить 1.6MB при 1254×1254 — працює, але важче за
  ідеал; в цьому середовищі немає інструменту стиснути зображення
  (ImageMagick/sharp не встановлені) — знадобиться зовнішній конвертер, якщо
  захочемо оптимізувати пізніше.
- **SEO-формулювання під варіанти запитів.** Користувач питав, як зловити
  запити типу «школа Сиваківці» / «Сиваковецька школа» / «Севаківська школа»
  (описка). Пояснено: одруківки Google виправляє сам, коли сутність вже
  впізнана — навмисно вписувати неправильне написання в контент шкідливо
  (виглядає як спам). А от слова «школа» не було взагалі ні в
  `<meta description>`, ні в OG/Twitter тегах, ні в клієнтському описі
  головної сторінки (тільки «гімназія») — це реальна прогалина, виправлено.
  Також `alternateName` в JSON-LD (`index.html`) розширено в масив:
  `["Сиваковецька гімназія", "Сиваковецька школа", "Школа с. Сиваківці"]`.
- **Порада щодо підняття в пошуку (потребує дій користувача, не коду):**
  1. Google Business Profile (business.google.com, з поштою школи) — головний
     важіль для локальних запитів, показ у Картах/локальному пакеті. Це має
     зробити людина з доступом до акаунту школи.
  2. Зворотні посилання зі старого сайту на Google Sites / Facebook-групи
     школи / сайту ОТГ — домен зовсім новий (з ~2026-08-28), довіри пошуковика
     ще нема.
  3. Наповнити `news.js` реальним контентом — досі порожній, Google любить
     сайти, що оновлюються.
  4. Нові домени сидять у «пісочниці» Google тижні-місяці незалежно від якості
     — це нормально, не ознака проблеми.
- Комітнуто разом із ручною правкою користувача в `Home.jsx` (бейдж
  «Гаряче харчування» → «Харчування»). Коміти: `398de8d`, `ebe2819`.

**Наступного разу ("завтра"):** ймовірно продовжимо з Google Business Profile
(покроково) або з наповненням `news.js`.

## ✅ Зроблено 2026-08-29 (продовження) — велика сесія: SEO-довершення, іконка,
## галерея з подіями, фото персоналу, мобільне меню, темна тема

Все нижче вже закомічено й запушено в `main` (репо `Oxygenor/siv_sk`), деплой
пройшов успішно на кожному кроці. **Правило сесії: користувач попросив завжди
пушити коміт одразу після створення, без запитання** — див. пам'ять
`feedback_always_push`.

**Іконка сайту.** Користувач дав `public/iconSite.png` (1254×1254, логотип
гімназії) — замінив емодзі 🏫 у hero-блоці Home.jsx на це зображення
(`.hero-art-icon`).

**Google Search Console.** Додано два `<meta name="google-site-verification">`
в `index.html` (користувач реєстрував властивість під двома акаунтами — обидва
теги лишені, конфлікту нема). Верифікацію пройдено, sitemap.xml надіслано,
запит на індексацію головної сторінки відправлено. Додано також OpenGraph/
Twitter card теги (використовують `iconSite.png` як прев'ю-картинку).

**Фотогалерея — велика переробка (3 ітерації):**
1. Спершу витягнув через Playwright (клік по будь-якому `<img>`/background-image
   на сторінці не бачить WebFetch — старий сайт на Google Sites, JS-рендер) одне
   фото на подію зі сторінок «Шкільне життя 2025-2026» та «2024-2025» — разом з
   49 фото із загальної `/фотогалерея` (без прив'язки до подій) це дало 493 фото.
2. Користувач помітив неправильні підписи (напр. фото ярмарки підписане
   «Олімпійський урок»). Причина: список подій для зіставлення позицій я вписав
   вручну і пропустив 4 реальні події зі сторінки 2025-2026. Виправив, взявши
   заголовки подій напряму зі сторінки (`<p class="zfr3Q CDt4Ke">`), а не з
   власноруч набраного списку — так неможливо щось пропустити.
3. Виявилось, що кожна подія на старому сайті має **не одне фото, а цілу
   карусель** (Google Sites carousel-віджет, кнопка `[aria-label="Next"]",
   потрібно клікати програмно через `el.click()` в `page.evaluate`, бо кнопка
   з'являється лише на hover і Playwright-клік по ній таймаутить). Написав
   скрипт, що клікає кожну карусель до зациклення (перше повторне фото = кінець)
   — це дало **441 фото замість 46**, кожне з полем `event` (без прив'язки до
   конкретної події лишились тільки справжні "каруселі-фотозвіти без заголовка"
   на сторінці 2024-2025, підписані за змістом як «Новорічна фотозона»).
   Стара "невизначена купа" фото (135+261) виявилась побайтово ідентичною новим
   каруселям (git сам визначив це як rename) — тобто то були ті самі фото, просто
   без підпису; видалені як дублікат.
   UI (`Gallery.jsx`): вибір альбому «Шкільне життя 2024-2025» / «...2025-2026»
   тепер показує другий рядок кнопок-подій (з `events.js` вже не бере — бере
   унікальні `event` з масиву фото), клік на подію фільтрує лише її фото.
   Розмір `public/gallery/`: ~122MB.

**Фото персоналу.** Адміністрація (2) і Педагогічний колектив (9) — раніше літера
замість фото. Знайшов на старому сайті `кадровий-склад/адміністрація` та
`.../педагогічний-колектив`, фото зіставлені за позицією (фото стоїть прямо перед
іменем/посадою). `staff.js` отримав поле `photo`, `About.jsx` показує `<img>` як
пріоритет, літеру — як fallback якщо фото нема.

**Мобільне меню — 4 окремі баги, усі виправлені:**
- Кнопка-гамбургер не мала z-index → бічна панель (position:fixed, пізніше в
  DOM) малювалась поверх неї, закрити меню було неможливо. Додав z-index,
  анімацію в хрестик, підложку-затемнення що закриває по кліку повз, Escape.
- `.navbar-nav` успадковував `justify-content: flex-end` з десктопного правила
  (там це вирівнює меню по правому краю рядка) — при вертикальній розкладці на
  мобільному це стало вирівнюванням по НИЗУ: розкриття категорії (більше висоти
  списку) виштовхувало верхні пункти вгору й за межі екрана замість того, щоб
  розсунути пункти вниз. Додав `justify-content: flex-start` для мобільної версії.
- Той самий баг заважав нормальній прокрутці (флексбокс+overflow+flex-end —
  відомий глюк, коли початок вмісту стає недосяжним для скролу); після фіксу
  `overflow-y: auto` (вже був у коді) запрацював правильно аж до останнього пункту.
- Кнопка закриття сиділа в звичайному рядку хедера (нижче за верхню адресну
  смужку), а сама панель починається з самого верху екрана — тому перше
  посилання опинялось вище за кнопку. Зробив кнопку `position: fixed` (top:14px;
  right:14px) коли меню відкрите — тепер завжди в правому верхньому куті панелі.

**Темна тема (системна, `prefers-color-scheme: dark` в `index.css`).** Футер і
верхня контактна смужка (`.navbar-top`) використовували `--color-primary-dark`
як свій постійний темно-синій фон — але ця сама змінна в темному режимі навмисно
переозначена на світлий відтінок (для іншої мети: колір кнопки `.btn-primary`
при наведенні). Через це на телефонах із системною темною темою футер і верхня
смужка ставали світло-блакитними серед темної сторінки. Виправлено: додано
окрему змінну `--color-brand-dark` (завжди темна, не залежить від теми) для цих
двох елементів; `--color-primary-dark` лишився вільним адаптуватись під тему
для кнопки, як і задумано.

**Дрібне:** плейсхолдер «Пошук по сайту…» не вміщався в поле пошуку на десктопі
(160px при 16px шрифті) — зменшив шрифт поля до 0.85rem і трохи розширив поле.

**Свідомо не робив:** новини (`news.js` порожній) — користувач попросив
пропустити на цій сесії, повернутись пізніше. Джерело для новин ще не визначено
(старий сайт теж не мав текстових новин — сторінка "Новини" там порожня в HTML).

## ✅ Зроблено 2026-08-29 — динамічні `<title>`/meta/JSON-LD

Технічне SEO-покращення (продовження роботи 2026-08-28): раніше всі сторінки мали
однаковий `<title>` і `<meta name="description">` з `index.html` — Google бачив би
всі сторінки з однаковою назвою й описом у видачі. Додано:

- `src/hooks/usePageMeta.js` — хук на `useEffect`, що на кожній сторінці виставляє
  унікальні `document.title` (формат «Заголовок — Сиваковецька гімназія»),
  `<meta name="description">` і `<link rel="canonical">` з фактичного `pathname`.
- Підключено всередину `PageHero.jsx` (title/lead передаються автоматично для 12
  сторінок, які його використовують) + вручну в `Home`, `NotFound`, `NewsDetail`,
  `CustomSection` (де своя логіка заголовка/опису, зокрема з тексту новини/розділу).
- В `index.html` додано JSON-LD (`schema.org/School`) зі статичними даними закладу
  (назва, адреса, телефон, email, Facebook) — для розширених сніпетів у Google.

Перевірено Playwright-скриптом (`npx playwright`, бо `chromium-cli` в цьому
середовищі не встановлений): `/`, `/pro-gimnaziyu`, `/uchnyam`, неіснуючий маршрут —
на кожній свій title/description/canonical, консоль без помилок. `npm run lint` і
`npm run build` чисті.

**Не зроблено (свідомо):** OpenGraph/Twitter-теги, prerendering/SSR для першого
пейнту (Googlebot виконує JS, тому client-side достатньо для GitHub Pages без
бекенда) — можна додати пізніше, якщо знадобиться соцшеринг зі превʼю.

## Деплой і домен (2026-08-28)

Сайт живий на GitHub Pages з кастомним доменом **https://syvakovetska.vn.ua/**
(репозиторій `Oxygenor/siv_sk`, публічний — приватні репо не підтримують безкоштовний
Pages). DNS (CNAME → oxygenor.github.io) розповсюджено й підтверджено.

**SEO:** перейшли з `HashRouter` на `BrowserRouter` (адреси тепер `/novyny` замість
`/#/novyny` — Google індексує лише частину URL до `#`, тож підсторінки не індексувались
би окремо). Для GitHub Pages (немає серверних рерайтів) `deploy.yml` копіює
`dist/index.html` → `dist/404.html` після білда — це стандартний трюк для SPA на
статичному хостингу. Побічний ефект: також полагодив якорне прокручування
(`/pro-gimnaziyu#administratsiya` тощо) — під HashRouter це було технічно неможливо
коректно (два `#` в одній адресі), тепер працює. Додано `public/robots.txt` і
`public/sitemap.xml` (перелічує лише статичні маршрути — коли додасте реальні новини
в `news.js` чи розділи в `sections.js`, сторінки `/novyny/:id` і `/rozdily/:slug`
варто буде дописати в sitemap вручну).

**Ще треба (користувачу):** зареєструвати сайт у Google Search Console
(search.google.com/search-console), додати властивість для `syvakovetska.vn.ua`,
підтвердити власність (через DNS TXT-запис або HTML-файл) і надіслати
`https://syvakovetska.vn.ua/sitemap.xml` на індексацію.

## Що вже зроблено

**Стек:** React 19 + Vite + react-router-dom. Сайт повністю статичний — без бекенда,
без бази даних, без адмін-панелі.

**Структура (src/data/nav.js)** — уже побудована й відповідає розділам оригінального сайту
(sites.google.com/view/sivshkola), перекладена в чисті маршрути: `/`, `/pro-gimnaziyu`,
`/novyny`, `/uchnyam`, `/batkam`, `/dokumenty`, `/biblioteka`, `/formy-navchannya`,
`/fotogalereya`, `/korysni-posylannya`, `/kontakty`.

**Створені файли:**
- `src/data/*.js` — nav, school (контакти), staff, events (шкільне життя 2025-2026), faq, links,
  documents (каталог "слотів" для документів — самих файлів ще нема), news, gallery, sections
  (усі три поки порожні масиви — контент ще не додано)
- `src/pages/*.jsx` — Home, News, NewsDetail, Students, Parents, About, Documents, Library, LearningForms, Gallery, Contact, Links, Search, Sections, CustomSection, NotFound
- `src/components/*.jsx` — Navbar, Footer, Layout, Breadcrumbs, NewsCard, QuickLinks, DocumentList, PageHero, Loader, ScrollToHash, Section
- `src/App.jsx`/`main.jsx` — реальний `HashRouter` + `Layout` + всі публічні сторінки. Перевірено 2026-08-27: запущено `npm run dev`, Playwright відкрив `/`, `/pro-gimnaziyu`, `/novyny`, `/kontakty` — реальний український контент рендериться, консоль без помилок.

## ✅ Зроблено 2026-08-28 — повне видалення Firebase

Користувач вирішив (спільно з колегою) відмовитись від Firebase повністю (не лише
Storage, як раніше, а й Auth і Firestore) — Firebase зараз платна для їхніх потреб.
Сайт перероблено на повністю статичний:

1. Видалено `src/firebase/`, `src/hooks/useAuth.js`, `src/hooks/useCollection.js`,
   `src/admin/` (уся адмін-панель і логін), `src/components/ProtectedRoute.jsx`,
   `firestore.rules`, залежність `firebase` з `package.json`, `.env.local`,
   `VITE_FIREBASE_*` з `.env.example` і з `.github/workflows/deploy.yml`.
2. Дані, що раніше жили у Firestore, стали статичними файлами:
   `src/data/news.js`, `src/data/gallery.js`, `src/data/sections.js` (нові, поки
   порожні масиви) та `src/data/documents.js` (додано опційне поле `filename` до
   кожного слоту документа).
3. Сторінки `News`, `NewsDetail`, `Gallery`, `Sections`, `CustomSection` та компонент
   `DocumentList` переписані на прямий імпорт зі `src/data/*` замість
   `useCollection(...)`. `NewsDetail`/`NewsCard` тепер форматують просте поле
   `item.date` (рядок) замість Firestore Timestamp (`.toDate()`).
4. Контактна форма (`Contact.jsx`) більше нічого нікуди не пише — кнопка
   «Надіслати» формує `mailto:` посилання на пошту школи (`school.email`) з
   темою й текстом листа і відкриває поштову програму відвідувача.
5. `README.md` переписано під нову модель: розділ "Як додати контент" замість
   Firebase-налаштування й адмін-панелі.

Перевірено того ж дня: `npm install`, `npm run build`, `npm run lint` — усе чисто;
Playwright пройшовся по всіх публічних маршрутах і підтвердив, що `/admin` тепер
віддає 404. Закомічено в `ea43ab6`.

## Firebase — більше не використовується

Раніше був Firebase-проєкт `school-d9c92` для Auth/Firestore адмін-панелі — це
рішення скасовано (див. вище), проєкт Firebase більше не потрібен і не
використовується сайтом.

## Аудит контенту оригінального сайту (Google Sites) — статус

Через фонові агенти (WebFetch) пройдено всі ~45 підсторінок, включно з розділом
"Здобувачам освіти" (перезапущено 2026-08-28 — попередній запуск зависав, цей пройшов
чисто за ~75 сек, схоже це був разовий збій).

### Розділ "Здобувачам освіти" (7 підсторінок) — результат аудиту 2026-08-28
Реальний навігаційний список (без 8-го пункту, якого не існує): Розклад уроків,
Правила поведінки здобувачів освіти, Критерії оцінювання, Обов'язки здобувачів освіти,
Протидія Булінгу, Психологічна підтримка, Дистанційне навчання.

**Текст вдалося витягнути повністю для 2 сторінок — виявилось, що він уже й так є в
`src/pages/Students.jsx` (доданий ще в початковому коміті, до цієї сесії), агент лише
підтвердив, що це справжній оригінальний текст:**
- **Обов'язки здобувачів освіти** — обидва пункти вже на сайті слово в слово.
- **Дистанційне навчання** — уже описано (Google Meet + Google Classroom, навіть
  повніше, ніж знайшов агент). Нічого вносити не треба.

**Для решти 5 сторінок текст заблоковано вбудованим Google Drive-контентом**
(WebFetch бачить лише іконку файлу/зображення, не вміст):
- **Розклад уроків** — файл **«розклад 23-24.docx»** (НЕ «СТРУКТУРА 2023.docx», як
  очікувалось раніше — можливо, це інший/новіший файл). Найвищий пріоритет — тут
  дзвінковий розклад.
- **Правила поведінки здобувачів освіти** — файл «правила поведінки.docx»
- **Критерії оцінювання** — Google Drive PDF (назва-хеш, без людської назви)
- **Протидія Булінгу** — 2 зображення (Drive), можливо скановані сторінки документів
- **Психологічна підтримка** — 2 зображення (Drive), можливо плакат/контакти психолога

Окремо (інша гілка навігації, "Безпека в школі → Стоп булінг") є ще «План заходів» і
«Порядок подання заяв про випадки булінгу» — точні slug ще не підтверджені, треба
перевірити окремо.

### Виявлена системна проблема
WebFetch бачить лише статичний HTML/markdown-знімок Google Sites — **пропускає JS-контент,
вбудовані зображення-документи та Google Docs/Drive вставки**. Це означає, що значна частина
реального вмісту НЕ витягнута текстово і потребує **ручного перегляду в браузері** перед тим,
як оригінальний сайт можна буде вимкнути:

- **Стрічка новин** (`/новини`) — динамічна, не захоплена
- **Наші досягнення** — конкретні тексти досягнень (олімпіади, конкурси, імена) не витягнуті
- **Файли .docx/.pdf на Google Drive**, які треба відкрити й переписати/завантажити вручну:
  - Кадрове забезпечення.docx
  - Правила прийому до З.О..docx
  - Матеріально-технічне забезпечення закладу освіти.docx
  - Статут, ліцензія (Google Drive папка)
  - СТРУКТУРА 2023.docx — **тут повний дзвінковий розклад**, критично для сторінки "Розклад уроків"
  - Положення про академічну доброчесність.docx
  - Стратегія розвитку закладу.docx
  - Річний план роботи 2023-2024.docx
  - Освітня програма 2023-2024н.р..pdf
  - штатний.pdf (штатний розпис)
  - Заходи по запобіганню булінгу.docx
  - порядок подання та розгляду заяв щодо булінгу.docx
  - Місячні плани заходів (лютий–березень) — вбудовані як зображення
- **Територія обслуговування** — список сіл/вулиць поданий як зображення карти (потрібен ручний перегляд/OCR)
- **Умови доступності для дітей з особливими потребами** — лише 4 фото, без тексту
- **Covid-19, Моніторинг якості освіти, Атестація учителів, Структура управління закладу** — сторінки порожні у витягнутому HTML, перевірити вручну чи там взагалі є контент
- **Фотогалерея** — ~50 фото без підписів/дат/назв альбомів (jpg-файли на lh3.googleusercontent.com)

### Уже здобуті конкретні факти (можна одразу вносити в код)
- Режим роботи закладу: **8:30–18:00**
- Ліцензований обсяг: **200 осіб**; фактично на 02.09.2024 — **87 учнів** (базова середня освіта — 47)
- Мова освітнього процесу: українська
- Структура навчального року 2024/2025 (перевірити, чи є оновлена на 2025/2026):
  І семестр 02.09.24–20.12.24, ІІ семестр 13.01.25–30.05.25,
  осінні канікули 28.10–03.11.24 (перенесено на 23.12–27.12 наказом №76),
  зимові 28.12.24–12.01.25, весняні 24.03–30.03.25, останній дзвоник 30.05.25
- Адміністрація: Дячук Наталія Михайлівна (директор), Ломачевська Наталія Анатоліївна (заступник з НВР)
- 9 вчителів з іменами та предметами — вже перенесено в `src/data/staff.js`
- Вакансій немає
- Повний список зовнішніх посилань (Корисні посилання) — вже в `src/data/school.js` / `src/data/links.js`, звірити на повноту

## Наступні кроки (у порядку)

1. ~~Створити репозиторій на GitHub і залити проєкт~~ — зроблено 2026-08-28:
   `https://github.com/Oxygenor/siv_sk`, гілка `main` запушена. Ще треба: увімкнути
   **Settings → Pages → Source → GitHub Actions** у репозиторії (користувач це зробить
   сам, коли буде готовий пушити далі).
2. ~~Додати в код текст "Обов'язки здобувачів освіти" та "Дистанційне навчання"~~ —
   перевірено 2026-08-28: цей текст уже є в `src/pages/Students.jsx`, вносити нічого
   не треба.
3. Відкрити вручну (потрібен Google-акаунт із доступом) і переписати вміст
   заблокованих Drive-файлів: «розклад 23-24.docx» (найважливіше — дзвінковий
   розклад), «правила поведінки.docx», PDF критеріїв оцінювання, 2 зображення з
   "Протидія Булінгу", 2 зображення з "Психологічна підтримка"
4. Пройтись по решті списку файлів/сторінок нижче (статут, ліцензія, кадрове
   забезпечення тощо)
5. Наповнити `src/data/news.js`, `gallery.js`, `sections.js` реальним контентом (див. розділ 2 README.md)
